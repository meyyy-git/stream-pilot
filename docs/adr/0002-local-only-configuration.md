# Keep Stream Pilot local-only

Stream Pilot works without an account or backend because its controls target an OBS instance on the local network and a single operator device. The OBS password and private Trakteer Action URLs remain in device secure storage, while non-sensitive layout preferences remain in local app storage; this avoids operating a credential-bearing cloud service at the cost of no cross-device synchronization.
